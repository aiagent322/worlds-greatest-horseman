# Champion pages are served at their extension-less URLs by Cloudflare Pages
# automatically (e.g. /champions/name_2022 serves champions/name_2022.html).
# The previous manual 301 rules created an infinite redirect loop with the
# platform's built-in .html -> extension-less 308, so they were removed.
